#include "Scene.hpp"

Scene::Scene()
{
    // The constructor can be empty for now.
    // We will create entities in ViewportWidget::initializeGL.
}

Scene::~Scene()
{
    // The registry will clean itself up automatically.
}